# Patches for Frappe Vault
# Listed in the order they run. Format: <module>.<path> — Frappe calls execute().

[pre_model_sync]

[post_model_sync]
# Needs the `database_type` column to exist, so it must run after the sync.
frappe_vault.patches.backfill_database_type
